{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "seed": "lerna run seed --stream",
    "setup:db:seed": "lerna run setup:db:seed --stream",
    "dev": "lerna run dev --stream",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream --parallel",
    "test": "yarn jest --coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^9.23.0",
    "jest": "^29.7.0",
    "lerna": "^8.2.1",
    "typescript": "^5.8.2"
  }
}
